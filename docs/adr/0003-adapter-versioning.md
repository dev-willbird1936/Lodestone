# ADR 0003: Adapter artifacts are version-specific

## Decision

Each active adapter targets an explicit loader and Minecraft version.

## Consequences

The stable protocol spans versions; native binaries do not pretend to be universal.
