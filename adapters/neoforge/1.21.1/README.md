# NeoForge 1.21.1 adapter

Primary pure native adapter for NeoForge 1.21.1. It implements command discovery/execution, player
state reads, block reads, bounded overworld writes, loaded entity queries, and server-player
inventory reads. It does not depend on the MCP gateway. The runnable loader host is
`hosts/neoforge/1.21.1`.
